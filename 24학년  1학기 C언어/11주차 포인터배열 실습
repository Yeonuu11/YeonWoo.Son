배열의 모든 값을 2씩 증가하여 전부 2씩 증가
#include <stdio.h>

int main (void){
	
	int arr[5] = {1,2,3,4,5};
	int *ptr = &arr[0];
	// int *ptr = arr;
	int i;
	
	for(i=0;i<5;i++){
		*ptr += 2;
		ptr++;
	}

 	for(i=0;i<5;i++)
	{
		*(ptr+i) += 2;
	} <<<< 이렇게 하면 ptr 위치가 변하지 않는다.

	for(i=0;i<5;i++){
		printf("%d ", arr[i]);
	}
	
	return 0;
}

#include <stdio.h>

int main (void){
	
	int arr[5] = {1,2,3,4,5};
	int *ptr = &arr[4];
	int i, total = 0;
	
	for(i=0;i<5;i++)
	{
		total += *ptr;
		ptr--;
	}


	printf("%d ", total);
	
	return 0;
}


배열의 값 거꾸로 출력
#include <stdio.h>

int main (void){
	
	int arr[6] = {1,2,3,4,5,6};
	int *ptr1 = &arr[0];
	int *ptr2 = &arr[5];
	int temp = 0, i;
	
	for(i=0;i<3;i++)
	{
		temp = *ptr1;
		*ptr1 = *ptr2;
		*ptr2 = temp;
		ptr1++;
		ptr2--;	
	}

	for (i = 0; i<6; i++)
		printf("%d ", arr[i]);
		
	return 0;
}
